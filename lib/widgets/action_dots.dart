import 'package:flutter/material.dart';

class ActionDots extends StatelessWidget {
  const ActionDots({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CircleAvatar(
      radius: 3,
      backgroundColor: Colors.white,
    );
  }
}
